static BinNode<int> NodeFromArray(int[] arr)
{
	
	# val, prev, next
	BinNode<int> orig = new BinNode<int>(null, null, null);
	BinNode<int> node = orig;
	
	for(int i = 0; i < arr.length; i++)
	{
		
		node.SetValue(arr[i]);
		
		BinNode<int> newNode = new BinNode<int>(null, node, null);
		
		node.setNext(BinNode);
		
		node = newNode;
	
	}
	
	return orig;


}